sequenceDiagram
    participant Browser
    participant Server

    Note right of Browser: User writes a note and clicks Save

    Note right of Browser: Javascript creates a new note and updates the page

    Browser->>Server: POST /new_note_spa
    activate Server
    Server-->>Browser: 201 created
    deactivate Server

    Note right of Browser: Browser keeps the page open and displays the new note